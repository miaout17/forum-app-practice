
<h2>New Post:</h2>

<%= link_to("New Attachment", new_attachment_path, :target => "_blank") %>

<div>
Board: <%= @board.name %>
</div>

<%= form_for([@board, @topic]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
<% end %>

